name: OrangeHRM Strategic Execution Suite

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser'
        required: false
        default: 'chrome'
        type: choice
        options: [chrome, firefox, edge]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

env:
  BROWSER: ${{ github.event.inputs.browser || 'chrome' }}
  MAVEN_OPTS: -Xmx2048m

jobs:

  foundation:
    name: Foundation (Foundation (Admin + PIM + Recruitment)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Start Display (Xvfb)
        run: |
          export DISPLAY=:99
          sudo Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3

      - name: Run Foundation Tests
        run: |
          mvn test -B \
            -Dsurefire.suiteXmlFiles=testng.xml \
            -Dtestnames=Step1_Hyper_Admin_PIM_Foundation,Step2_PIM_User_Foundation,Step3_Recruitment \
            -Dbrowser=chrome \
            -Dheadless=true \
            -Dmaven.test.failure.ignore=true
        env:
          DISPLAY: :99

      - name: Upload Foundation Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-foundation
          path: target/allure-results
          retention-days: 5

  operations:
    name: Ops - ${{ matrix.name }}
    needs: foundation
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Time_Core
            testname: "Step4_Ops_Time_Core"
          - name: Time_Logic
            testname: "Step4_Ops_Time_Logic"
          - name: Leave_Config
            testname: "Step4_Ops_Leave_Config"
          - name: Leave_Apply
            testname: "Step4_Ops_Leave_Apply"
          - name: Claims
            testname: "Step4_Ops_Claims"
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Start Display (Xvfb)
        run: |
          export DISPLAY=:99
          sudo Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3

      - name: Run ${{ matrix.name }}
        run: |
          mvn test -B \
            -Dsurefire.suiteXmlFiles=testng.xml \
            -Dtestnames="${{ matrix.testname }}" \
            -Dbrowser=chrome \
            -Dheadless=true \
            -Dmaven.test.failure.ignore=true
        env:
          DISPLAY: :99

      - name: Upload Ops Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-ops-${{ matrix.name }}
          path: target/allure-results
          retention-days: 5

  performance:
    name: Perf - ${{ matrix.name }}
    needs: foundation
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Perf_Reviews
            testname: "Step5_Perf_Reviews"
          - name: Perf_Eval
            testname: "Step5_Perf_Evaluation"
          - name: Buzz_Dash
            testname: "Step5_Perf_Buzz_Dash"
          - name: Dash_Adv
            testname: "Step5_Perf_Dash_Adv"
          - name: All_Reports
            testname: "Step5_Perf_Reports"
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Start Display (Xvfb)
        run: |
          export DISPLAY=:99
          sudo Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3

      - name: Run ${{ matrix.name }}
        run: |
          mvn test -B \
            -Dsurefire.suiteXmlFiles=testng.xml \
            -Dtestnames="${{ matrix.testname }}" \
            -Dbrowser=chrome \
            -Dheadless=true \
            -Dmaven.test.failure.ignore=true
        env:
          DISPLAY: :99

      - name: Upload Perf Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-perf-${{ matrix.name }}
          path: target/allure-results
          retention-days: 5

  maintenance:
    name: Maint - ${{ matrix.name }}
    needs: [operations, performance]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Auth_Records
            testname: "Step6_Maintenance_Auth"
          - name: Safety_Purge
            testname: "Step6_Maintenance_Safety"
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Start Display (Xvfb)
        run: |
          export DISPLAY=:99
          sudo Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3

      - name: Run ${{ matrix.name }}
        run: |
          mvn test -B \
            -Dsurefire.suiteXmlFiles=testng.xml \
            -Dtestnames="${{ matrix.testname }}" \
            -Dbrowser=chrome \
            -Dheadless=true \
            -Dmaven.test.failure.ignore=true
        env:
          DISPLAY: :99

      - name: Upload Maint Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-maint-${{ matrix.name }}
          path: target/allure-results
          retention-days: 5

  cleanup:
    name: Clean - ${{ matrix.name }}
    needs: [operations, performance]
    if: always()
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Admin_PIM
            testname: "Step7_Clean_Admin_PIM"
          - name: Recruit_Perf
            testname: "Step7_Clean_Recruit_Perf"
          - name: Others
            testname: "Step7_Clean_Others"
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Start Display (Xvfb)
        run: |
          export DISPLAY=:99
          sudo Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3

      - name: Run ${{ matrix.name }}
        run: |
          mvn test -B \
            -Dsurefire.suiteXmlFiles=testng.xml \
            -Dtestnames="${{ matrix.testname }}" \
            -Dbrowser=chrome \
            -Dheadless=true \
            -Dmaven.test.failure.ignore=true
        env:
          DISPLAY: :99

      - name: Upload Cleanup Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-clean-${{ matrix.name }}
          path: target/allure-results
          retention-days: 5

  report:
    name: Generate Report
    needs: [foundation, operations, performance, maintenance, cleanup]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download All Results
        uses: actions/download-artifact@v4
        with:
          path: all-results
          pattern: allure-*
          merge-multiple: true

      - name: Download Allure History (Previous Report)
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Merge Results & Restore History
        run: |
          mkdir -p target/allure-results
          cp -r all-results/* target/allure-results/ || true
          if [ -d "gh-pages/history" ]; then
            echo "Found previous history, restoring..."
            cp -r gh-pages/history target/allure-results/
          else
            echo "No previous history found (First run?)"
          fi

      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: target/allure-results
          allure_history: gh-pages
          keep_reports: 30

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-report

      - name: Job Summary
        if: always()
        run: |
          echo "## Test Execution Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Step | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Foundation | ${{ needs.foundation.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Operations | ${{ needs.operations.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Performance | ${{ needs.performance.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Maintenance | ${{ needs.maintenance.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Cleanup | ${{ needs.cleanup.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Allure Report:** [View Report](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/)" >> $GITHUB_STEP_SUMMARY